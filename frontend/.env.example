# Base URL of the backend API. In local dev with the Vite proxy above you can
# leave this as /api. When deployed separately (e.g. frontend on Vercel,
# backend on Render), point this at the full backend URL, e.g.
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=/api
